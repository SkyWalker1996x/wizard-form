import styled from 'styled-components';
import { FlexWrapper } from 'UI/FlexWrapper';
import { Text } from 'UI/Text';

export const UserListWrapper = styled(FlexWrapper)`
  padding: 50px 235px;
`;

export const UserListTitleWrapper = styled(Text)`
  margin-bottom: 25px;
`;
